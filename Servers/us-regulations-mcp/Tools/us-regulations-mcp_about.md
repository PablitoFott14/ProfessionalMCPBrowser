## us-regulations-mcp_about

### What this tool is for
Server metadata, dataset statistics, freshness, and provenance information. Use this at the start of a session when the user needs to verify the server's coverage and data basis before relying on its regulatory outputs.

---

### Used parameters

This tool takes no parameters.

---

### JSON input alternatives

```json
{
  "intent": "Review server coverage and freshness information before starting compliance research",
  "params": {}
}
```
